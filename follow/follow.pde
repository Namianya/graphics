int[] xpos = new int[50];
int[] ypos = new int[50];
void setup() {
    size(200,200);
    smooth();
    for (int i = 0; i < xpos.Length; ++i) {
        xpos[i] = 0;
        ypos[i] = 0;
    }
}

void draw() {
    background(255);
    for (int i = 0; i < len; ++i) {
        
    }
}

